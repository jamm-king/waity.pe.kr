<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.waity.api.mapper.kingtagMapper">
    <select id="selectKingtagAll" resultType="com.waity.api.dto.kingtagDTO">
		SELECT
				A.tag_ptr_id as id, B.tag_name, A.parent_tag_id
		FROM
				yt_kingtag A
		INNER JOIN
				yt_tag B
		ON
				A.tag_ptr_id = B.id
    </select>
    <select id="selectKingtagByNames" resultType="com.waity.api.dto.kingtagDTO">
		select
				B.id, B.tag_name, A.parent_tag_id
		from
				yt_kingtag A
		INNER JOIN
				yt_tag B
		ON
				A.tag_ptr_id = B.id
		WHERE
				B.tag_name IN
				<foreach item="name" index="index" collection="array" open="(" close=")" separator=",">
					#{name}
				</foreach>
    </select>
	<insert id="insertKingTag" parameterType="com.waity.api.dto.kingtagDTO">
		INSERT INTO
				yt_kingtag (tag_ptr_id, parent_tag_id)
		VALUES(
				#{id},
				#{parentTagId}
		)
		ON DUPLICATE KEY UPDATE
				tag_ptr_id = #{id}, parent_tag_id = #{parentTagId}
	</insert>
	<update id="updateKingTag" parameterType="com.waity.api.dto.kingtagDTO">
		update
				yt_kingtag
		set
				parent_tag_id = #{parentTagId}
		where
				tag_ptr_id = #{id}
	</update>
	<delete id="deleteKingTag" parameterType="com.waity.api.dto.kingtagDTO">
		delete from
				yt_kingtag
		where
				tag_ptr_id = #{id}
	</delete>
</mapper>