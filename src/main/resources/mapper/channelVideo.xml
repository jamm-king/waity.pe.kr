<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.waity.api.mapper.channelVideoMapper">
    <select id="selectVideoByChannel" resultType="com.waity.api.dto.videoDTO">
        SELECT
            C.id, C.video_id, C.title, C.thumbnail
        FROM
            yt_channel A
        INNER JOIN
            yt_channel_video B
        ON
            A.id = B.channel_id
        INNER JOIN
            yt_video C
        ON
            C.id = B.video_id
        WHERE
            A.id = #{channelId}
    </select>
    <select id="selectVideoByChannels" resultType="com.waity.api.dto.videoDTO">
        SELECT
            C.id, C.video_id, C.title, C.thumbnail
        FROM
            yt_channel A
        INNER JOIN
            yt_channel_video B
        ON
            A.id = B.channel_id
        INNER JOIN
            yt_video C
        ON
            C.id = B.video_id
        WHERE
            A.id IN
            <foreach item="id" index="index" collection="array" open="(" close=")" separator=",">
                #{id}
            </foreach>
    </select>
    <delete id="deleteVideoByChannel" parameterType="int">
        delete
            A, B
        from
            yt_video A
        inner join
            yt_channel_video B
        on
            A.id = B.video_id
        where
            B.channel_id = #{channelId}
    </delete>
</mapper>